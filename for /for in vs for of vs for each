for & for in 

const arr = ['a', 'b', 'c'];

for (let i = 0; i < arr.length; ++i) {
  console.log(arr[i]);
}

for (let i in arr) {
  console.log(arr[i]);
}

access only the array index 

for of & for each 

arr.forEach((v, i) => console.log(v));

for (const v of arr) {
  console.log(v);
}

forEach() and for/of get access to the array element itself,
forEach() you can access the array index i,
